# air: ファイル保存を検知して自動で再ビルド・再起動する
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  # 保存直後の連続ビルドを防ぐ待ち時間(ms)
  delay = 300
  stop_on_error = true

[log]
  time = true
